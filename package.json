{
    "name": "quitmate",
    "private": true,
    "workspaces": ["apps/*", "packages/*"],
    "scripts": {
      "dev:web": "turbo run dev --filter=web",
      "dev:lp": "turbo run dev --filter=lp"
    },
    "devDependencies": {
      "turbo": "^1.12.0"
    }
  }